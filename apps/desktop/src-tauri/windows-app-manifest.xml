<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<!--
  Application manifest embedded into VideoDubber.exe (see build.rs).

  This REPLACES tauri-build's default manifest, so the Common-Controls
  dependency below must stay: dropping it regresses every native dialog to the
  Windows 95 look.

  Two additions on top of the default, both of which the app needs and neither
  of which is visible on macOS:

  * longPathAware — user data lives under %USERPROFILE%\VideoDubber, and a
    project path is <profile>\VideoDubber\projects\<project id>\<stage>\... with
    per-segment audio files named after the segment. Nested under a long user
    name or a redirected OneDrive profile that clears MAX_PATH (260 chars) fast,
    and a narrow-API caller that hits it fails with a bare "path not found".

  * activeCodePage UTF-8 — the same reason PYTHONUTF8/PYTHONIOENCODING are set
    for every sidecar (sidecar.rs): Vietnamese is the primary target language, so
    project names, voice names ("Ngọc Lan") and transcript text are routinely
    non-Latin-1. Without this, narrow-char Win32 APIs interpret them in the
    machine's ANSI codepage (cp1252 on a Western install) and mangle them.
    Honoured on Windows 10 1903+ and ignored harmlessly on older builds.
-->
<assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
  <dependency>
    <dependentAssembly>
      <assemblyIdentity
        type="win32"
        name="Microsoft.Windows.Common-Controls"
        version="6.0.0.0"
        processorArchitecture="*"
        publicKeyToken="6595b64144ccf1df"
        language="*"
      />
    </dependentAssembly>
  </dependency>
  <application xmlns="urn:schemas-microsoft-com:asm.v3">
    <windowsSettings>
      <longPathAware xmlns="http://schemas.microsoft.com/SMI/2016/WindowsSettings">true</longPathAware>
      <activeCodePage xmlns="http://schemas.microsoft.com/SMI/2019/WindowsSettings">UTF-8</activeCodePage>
    </windowsSettings>
  </application>
</assembly>
